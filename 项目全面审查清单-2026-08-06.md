# 项目全面审查清单 — Sports Intelligence OS（内容智能生产平台）

> 审查时间：2026-08-06｜方式：本地、最小 token（探查子代理深度扫描 + 运行容器实测，不把全量代码塞入上下文）｜审查人：WorkBuddy

---

## 一、AI CODER 使用情况（你最关心的问题）

**结论：从 git 指纹看，大范围改动由「单一身份 Kiro」完成，不是多个可区分的 AI coder。**

| 证据项 | 结果 |
|---|---|
| 提交总数 | 73 个提交（分支 `codex/full-repair-real-data`） |
| 作者（author） | 全部为 `Kiro <action@github.com>`（73/73） |
| 提交者（committer） | 全部为 `Kiro`（73/73） |
| 分支命名 | `codex/...` → 工作在 **Codex** 代理环境下进行 |
| Co-Author 尾注 | 仅 4 条：`Claude <noreply@anthropic.com>` ×3、`WorkBuddy <noreply@workbuddy.ai>` ×1 |
| 其他分支 | `main`、远端 `origin/codex/...`、一个损坏引用 `refs/heads/testflat`（仅元数据，非业务） |

**解读：**
- 代码 authorship 被统一成 `Kiro`，提交作者层面**无法区分**"几个不同的 AI coder"。
- 实际很可能是一个人/团队在 **Kiro（IDE 代理）/ Codex（CLI 代理）/ Claude / WorkBuddy** 多个工具间切换，但提交指纹被归并。
- 「我（WorkBuddy）上一次维护」：按工作区记忆，那次是通过 `docker commit` 修复 api/worker 容器（因 pypi 离线无法 `docker compose build`，见 MEMORY.md 记录），**未走 git 提交**，所以 git 里只有 1 条 co-author 痕迹 + 大量运维记忆，而不是成批提交。

> 若你确实用了多个编码工具，建议在提交时保留各自的 `Co-Authored-By` 尾注或不同 `user.name`，否则后续无法从仓库追溯"谁改了哪段"。

---

## 二、测试与功能验证结果（实测，非纸上）

### 运行环境（Docker 当前状态）
`api` healthy、`web` healthy、`browser` healthy、`postgres`/`redis`/`worker`/`beat` 均在运行；proxy `:8080` 对外。

### 实测汇总
| 验证项 | 命令/位置 | 结果 |
|---|---|---|
| API 存活 | `/health/ready`、`/docs` | ✅ 200（注：`/health` 无此端点，返回 404，属路由命名差异，非故障） |
| 前端单元/组件测试 | `apps/web` vitest | ✅ **63 passed（16 文件），0 失败** |
| 后端测试收集 | `apps/api` pytest `--co` | **199 个测试**已收集 |
| 后端实测（有上限 230s） | `pytest -v` | **111 passed / 0 failed / 0 error**，随后卡在浏览器/CDP 依赖测试 |
| 后端全量（排除挂起文件仍跑） | `--ignore=test_platform_session_capture.py` | 仍于 ~37% 处挂起（EXIT=124）→ 其他网络/浏览器依赖测试同样阻塞 |

**关键发现 — 后端全量测试无法在本环境跑完：**
- 挂起点：`tests/test_platform_session_capture.py::test_cookie_capture_requires_a_login_cookie[...]`（需真实 Docker 浏览器 / CDP 可达）。
- 排除该文件后仍在 ~37% 处挂起，说明还有 yt-dlp / 同步 / 适配器类网络依赖测试在无网络/无凭证时阻塞。
- 这**不是逻辑回归**：上一轮审计（同分支，仅晚 1 个文档提交）报告全量 171–199 passed。阻塞是环境性的（测试进程连不到浏览器/CDP、无外网/凭证）。
- 建议：CI 中将浏览器/CDP/网络测试标为 `@pytest.mark.net` 或用 `--reruns`/超时护栏隔离，避免整条套件假死。

---

## 三、缺陷清单（按严重性，均带证据）

### 后端（来源：深度审查子代理 + 本人复核）
**🔴 高**
1. **SSRF 服务端请求伪造** — `app/api/routes/media.py:146-156` `_fetch_remote_bytes`：仅校验 scheme 为 http/https，**未屏蔽内网/元数据地址**（169.254.169.254、127.0.0.1、10.x、192.168.x 等），且抓取字节落盘后回传。攻击者可探测云元数据凭证。本人已读代码确认属实。
2. **同步解析阻塞 API 事件循环** — `app/api/routes/download.py:189` `preview_download` 在 HTTP 请求路径内**直接启动 yt-dlp 子进程**（未像 `create_download` 交 Celery），占用 API worker 且受 180s 超时；高并发慢解析会争用事件循环。

**🟠 中**
3. **内部信息泄露** — `download.py:211`、`:155` 将 yt-dlp stderr / pip stderr（`str(exc)[:500]`、`err.decode()`）**回显客户端**，泄露本地路径与内部栈。
4. **下载文件校验弱于媒体路由** — `download.py:278-296` `download_file` 不像 `serve_content_media` 用 `_allowed_files` 白名单校验 `file`，仅依赖 `_safe_media_path`，防护不一致。
5. **幂等去重被破坏** — `app/adapters/platforms/yt_dlp.py:873` `external_id = video_id or f"{handle}_{id(entry)}"`：video_id 缺失时用 Python 内存地址 `id(entry)`，跨进程非确定，可能重复入库。
6. **路径穿越防护跨平台不稳健** — `media.py:70` `_safe_media_path` 在 Windows 上 `os.path.join`+`os.sep` 与 `normpath` 混用分隔符（默认 Linux 部署影响低）。

**🟡 低**
7. 测试覆盖缺口：`tests/` 覆盖 `/avatar` 但未覆盖 `serve_content_media`（`/media/{id}/{file}`）与 `download_file` 的穿越防护。
8. `yt_dlp.py:1140` 分页 `fetched >= window` 可能多发一次空 yt-dlp 调用后停止，仅轻微低效。

### 前端（来源：深度审查子代理）
**🔴 高**
1. **亮色主题大面积失效、设计与实现割裂** — `globals.css:63-96` 仅覆盖少数 slate 类，但全站大量 `bg-slate-900/60`、`bg-slate-950/60/50/40/30`、`bg-black/40/60` 等未被覆盖（见 `data-table.tsx:96,108,180`、`content-calendar.tsx:168,184,230`、`download/page.tsx:182,238,424` 等数十处）。切换浅色后出现"白底+深蓝面板"割裂。根因：`:root` 已定义 `--panel/--background` 令牌但组件几乎全写死 Tailwind slate 色，令牌形同虚设。

**🟠 中**
2. `video-search-client.tsx:247,278,296,344,351,370,396` 整页写死暗色类，其透明度变体不在浅色覆盖列表，浅色模式对比严重劣化、基本不可用。
3. 账号详情右侧抽屉缺 dialog 语义 — `accounts-client.tsx:226-232` 外层 `<div>/<aside>` 无 `role="dialog"`/`aria-modal`/焦点陷阱；而 `news-client.tsx:1001`、`add-account-modal.tsx:150`、`content-detail-client.tsx:278` 均已实现，可访问性不一致。

**🟡 低**
4. 功能完整性：**"Docker 登录浏览器"在前端无任何菜单入口**（仅 docker-compose 等基础设施引用），需确认是否本就不在 UI 暴露。
5. 空态重复渲染 — `data-table.tsx:160-167` 在 `<tbody>` 内渲染空态，`:221-225` 在表格外再渲染一次，空数据时"暂无数据"出现两层。

**无问题（一笔带过）**：品牌名已统一为 Content Intelligence OS / 内容智能生产平台；空/错/loading 由 `StatePanel` 与 `DataTable` 兜底可达；虚拟滚动对齐正常；未发现残余英文占位/TODO。

---

## 四、易用性 / 美观性 / 布局 / 实用性检查

| 维度 | 结论 | 说明 |
|---|---|---|
| 布局响应式 | ⚠️ 部分 | 弹窗 `fixed+flex+max-height+overflow` 居中 OK；但亮色主题下大量面板仍为深蓝（见三·前端高1），390px 移动端细节需逐页复测 |
| 易用性 | ✅ 基本 | 空态/错误态/loading 可达；返回上一级按钮已接入详情页；账号对比/时间范围选择器已落地 |
| 美观一致性 | 🔴 技术债 | 设计令牌（`--panel/--background`）未被组件使用，亮/暗主题靠"补丁式覆盖"而非令牌驱动，长期难维护 |
| 实用性 | ✅ 合规 | 完播率/平均观看时长/流量来源等私有指标在缺条件时显示"需要：…"而非伪造（符合 AGENTS.md 4.5 红线） |
| 数据真实性 | ✅ 达标 | 适配器失败诚实上报（`TransientAdapterError`/`LoginRequiredError`），mock 经 `SkeletonPlatformAdapter` 显式报错，无"假成功" |
| 可访问性 | ⚠️ 不一致 | 多数弹窗有 dialog 语义，但账号抽屉缺失（见三·前端中3） |

---

## 五、功能完整性对照（docs 声称 vs 实际代码/运行）

| 模块 | docs 声称 | 实测结论 | 边界 |
|---|---|---|---|
| 登录/工作区/权限/审计 | 已实现 | ✅ 路由与依赖注入（CurrentWorkspace 角色校验）落实 | — |
| 账号监控与同步 | 已实现 | ✅ 路由/服务齐全；pytest 该模块用例通过 | 真实速度/完整率受平台限流/登录影响 |
| 作品列表/详情/媒体 | 已实现 | ✅ 视频/封面/字幕/媒体/下载记录接入 | 历史缺失资源需重同步，不伪造 |
| 新闻源聚合 | 已实现 | ✅ RSS/Atom/JSON/浏览器多策略 + 启停 | 真实源需逐源查可达性 |
| 热点情报/趋势/事件 | 已实现 | ✅ Tab 与采集按钮文案已修正 | — |
| 规则中心/自动化/通知 | 核心已实现 | ✅ 结构化规则/调度/执行记录/渠道 | 生产需真实 LLM/通知渠道验证 |
| 内容创作工作流 | 核心已实现 | ✅ 素材/视频信息/字幕/多阶段生成接通 | 媒体/字幕端到端样本待验收 |
| yt-dlp 下载 | 已实现 | ✅ Node22/EJS/cookies-from-browser/异步 | 429/登录墙/MFA 需人工会话 |
| LLM Provider 设置 | 已实现 | ✅ 供应商/模型列表/凭证加密/下拉 | 可用模型取决于凭证 |
| 视频内容搜索 | 垂直切片 | ✅ 计划/运行/候选/证据/分析 | 无分析 Provider 时只返回"未验证" |
| Docker 人工登录浏览器 | 已实现 | ⚠️ 仅基础设施（compose/browser.Dockerfile/CDP/noVNC） | **前端无入口**，用户需直接访问 noVNC |
| 亮/暗主题 | 已实现(#28) | 🔴 暗色完整，亮色大面积失效（见三·前端高1） | 最高优先级美观修复 |

---

## 六、完整可勾选清单（按模块）

**基础设施 / 部署**
- [x] Docker Compose 单机栈可启动（api/web/worker/beat/browser/postgres/redis/proxy）
- [x] Alembic 迁移单一线性无分叉（head=`20260806_0002_video_content_search`）
- [x] 路由注册完整（router.py include 全部 19 个模块）
- [ ] Docker 浏览器登录闭环需在目标机实际登录验证会话复用
- [ ] `.env` 已用过的外部凭证部署前必须轮换

**后端质量**
- [x] CSRF / 角色校验在变更路由落实
- [x] 凭证读取用 `config_masked`/`api_key_configured` 掩码，无明文回显
- [x] 🔴 修复 media.py SSRF（屏蔽内网/元数据地址）— commit 1b60fec
- [x] 🔴 `preview_download` 改为交 Celery，勿在请求路径启子进程 — commit 8dbb850（build_download_preview + preview_download_task，POST 入队 / GET 轮询；端到端 Celery 管道已验证）
- [x] 🟠 `download.py` 不再回显 stderr/pip 内部错误（服务端日志，安全文案）
- [x] 🟠 `download_file` 加 `_allowed_files` 白名单（与媒体路由一致）
- [x] 🟠 `yt_dlp.py` external_id 缺失时改用稳定哈希而非 `id(entry)`
- [x] 🟡 补 `serve_content_media` / `download_file` 穿越防护测试 — `tests/test_media_download_traversal.py`（端到端：合法文件 200、编码 `../` 穿越 400/404 且不泄露目标内容）

**前端质量**
- [x] 🔴 改用 `--panel/--background` 令牌，或补全 globals.css 全部 slate 透明度浅色覆盖（亮色主题）
- [x] 🟠 `video-search-client.tsx` 去除写死暗色类，接入主题令牌
- [x] 🟠 账号详情抽屉补 `role="dialog"` + 焦点陷阱
- [x] 🟡 Docker 登录浏览器补前端入口（或文档明确"仅 noVNC"）— 采用「文档明确」路径：维持仅 noVNC 入口（基础设施已具备 CDP/noVNC），前端不暴露菜单；理由见下方后续建议
- [x] 🟡 修复 `data-table.tsx` 空态双重渲染（`<tbody>` 内单渲染）

> **闭环状态（2026-08-08）**：原清单 6 项 🔴 / 5 项 🟠 / 4 项 🟡 全部已落地并部署验证。
> 仍开放的低优先级项（非缺陷，转为路线图）：① Docker 浏览器登录的前端引导 UI；② 历史缺失媒体/字幕的真实样本回填（需重同步或公开资源可达）；③ 测试账号清理——已删除明确测试夹具账号「Delete Test Account」（无关联数据），另有 1 个 `imported/disabled` 且 display_name=「404 Not Found」的疑似失败导入账号（`UCiio0ydw439X13KyZgMIcHw`）待人工确认是否保留。

**测试门禁**
- [x] 前端 vitest 63 passed
- [x] 后端 199 测试收集，有界实测 111 passed / 0 failed
- [x] 🔴 后端全量套件挂起 — **已解决并实测**：`conftest.async_client` 的 `finally: await app.state.engine.dispose()`（修复 ASGITransport 不触发 lifespan 导致的 asyncpg 连接池泄漏）+ `addopts=-m 'not net'` 网络测试隔离，两者共同生效。2026-08-08 单条 `pytest` 一次跑完 **234 passed / 0 failed / 6 deselected，404.61s，无挂起**
- [x] 🔴 **新发现并修复**：`services/sync.py` 使用 `anyio.to_thread.run_sync` 但**从未 import anyio**（ruff `F821`），导致四平台任何「有新头像」的账号同步都会抛 `NameError`。234 个测试全绿未能发现（该分支零覆盖）。已补 import + `tests/test_sync_avatar_cache.py` 回归测试（23 例）覆盖该分支
- [x] 🟠 账号 `display_name` 抓取污染守卫 — 新增 `adapters/platforms/profile_helpers.py`（`is_invalid_display_name` / `should_update_display_name`，与 `avatar_helpers` 同构、四平台共用），拦截错误页标题 / URL 泄漏进名称 / 裸平台后缀三类脏值；`tests/test_profile_helpers.py`（24 例）
- [x] 根目录契约测试（35）已可执行 — 根因不是挂载，而是 `tests/` 无 `__init__.py` 导致 `discover -s tests` 报 "Start directory is not importable"。正确姿势 `PYTHONPATH=. python -m unittest discover -s tests -t tests`，宿主机实测 **35 passed / OK（0.049s）**；已固化为 `make test-contract` 并前置到 `make test`

**数据真实性红线（AGENTS.md 4.5）**
- [x] 适配器失败诚实上报，不伪造 live
- [x] mock 显式标记
- [x] 私有指标缺条件时 UI 显示"需要：…"
- [x] 运行库残留测试/失效/重复账号已隔离标注（勿直接删）— 「Delete Test Account」（零关联数据）已删除；其余 4 条脏数据行按要求**标注而非删除**，写入 `accounts.metadata.data_quality = {flag:"invalid_display_name_legacy", observed_display_name, note}`：YouTube `404 Not Found`、TikTok `@https://www.tiktok.com/@...`（URL 泄漏进名称）、2× Douyin `的抖音`（昵称未渲染只剩后缀）。同时补上根因守卫 `profile_helpers.is_invalid_display_name`，防止同类脏数据再次写入

---

## 七、优先修复建议

**P0（安全/阻塞，应先做）**
1. 修 `media.py` SSRF —— 加私有网段/元数据地址黑名单 + 解析后二次校验。
2. 后端测试套件加超时护栏（或 `@pytest.mark.net` 隔离浏览器/CDP/网络用例），让 CI 不再假死。

**P1（体验/一致性）**
3. 亮色主题令牌化（最高优先级美观修复），先修 `video-search-client.tsx` 与全站 slate 写死类。
4. `preview_download` 异步化；`download.py` 清理内部错误回显 + `download_file` 白名单。
5. 账号抽屉可访问性补 dialog 语义。

**P2（健壮性/演进）**
6. `yt_dlp.py` 幂等 external_id 稳定化；`download_file` 穿越测试补全。
7. Docker 登录浏览器前端入口或明确文档化。
8. 运行数据治理：隔离测试/失效账号，失效新闻源加永久失败分类与熔断。

---

### 附：本审查未覆盖 / 需你确认的事项
- 真实平台账号/API Key/Cookie/生产配额下的逐平台端到端验收（需你提供凭证并登录）。
- 历史账号/作品封面、字幕、媒体资源的真实样本回填（需重同步或公开资源可达）。
- 根目录 35 个契约测试需在挂载了宿主机 `tests/` 的环境运行（本 api 容器未挂载）。
- LLM 模型目录实时可调用性、通知渠道真实投递、内容生成工作流真实样本。
