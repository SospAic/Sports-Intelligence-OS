# Sports Intelligence OS — Codex 长期执行约束

本文件是仓库级长期上下文。任何后续开发会话在修改代码前，都必须先阅读本文件以及 `docs/PROJECT_CONTEXT.md`、`docs/DELIVERY_PLAN.md` 和 `docs/STATUS.md`。

## 1. 当前项目目标

Sports Intelligence OS 面向体育短视频创作者、体育内容运营者和多平台账号管理者。系统需要把多平台账号与作品监控、体育新闻与热点、结构化规则与 Prompt、AI 内容生成、实时自动化与多渠道推送组合成一个可启动、可登录、可操作、可审计的完整产品。

首期是可工作的垂直切片，不接受用大量静态页面、硬编码返回值或无法追溯的假数据代替真实功能。

## 2. 强制执行顺序

后续工作严格按下列阶段推进，不得跳阶段，也不得把后续阶段宣称为已完成：

1. Prompt 00：项目总控与长期上下文
2. Prompt 01：项目规划与架构设计
3. Prompt 02：初始化项目与基础设施
4. Prompt 03：统一账号和作品数据模型
5. Prompt 04：平台适配器与监控任务
6. Prompt 05：体育新闻聚合系统
7. Prompt 06：7.9 规则中心与可视化编辑器
8. Prompt 07：Prompt 工作流与内容生成系统
9. Prompt 08：实时规则引擎与推送系统
10. Prompt 09：前端管理后台
11. Prompt 10：测试、文档与验收
12. Prompt 11：最终代码审查与修复

只有用户提交相应 Prompt 后，才能进入下一阶段。当前阶段记录在 `docs/STATUS.md`。

## 3. 每个阶段的固定工作协议

每次开始阶段开发时必须：

1. 检查现有代码、仓库状态、配置、迁移和相关文档；若存在用户未提交修改，必须保留并绕开无关改动。
2. 明确本阶段范围、依赖、验收标准和不在范围内的内容。
3. 在不破坏已实现功能的前提下完成最小闭环；平台、数据源、模型和通知差异必须进入 Adapter 或 Provider。
4. 运行与改动成比例的单元、集成或端到端测试；修复由本阶段暴露或引入的错误。
5. 更新 README、接口/配置说明、迁移说明和 `docs/STATUS.md` 中受影响的部分。
6. 阶段交付报告必须列出：完成内容、验证命令与结果、真实数据/模拟数据边界、已知限制、尚未实现内容、下一阶段入口。

不得因为测试环境缺少外部凭证而伪造“真实平台成功”。无凭证时应使用明确标识的 Mock 适配器完成契约测试，并把真实集成标为未验证。

## 4. 数据真实性与来源追踪

所有外部数据必须能区分以下来源：

- `live`：通过官方 API、RSS 或获准的公开接口实时获取；
- `imported`：用户导入且保留原始文件或来源信息；
- `mock`：只用于开发、演示或自动化测试的合成数据。

Mock 数据必须在适配器名、响应元数据和 UI 中显著标记，禁止显示为真实平台数据。生产数据实体应保留可审计的来源信息，至少包括 `source_kind`、`provider`、`external_id`、`fetched_at`，适用时还包括 `source_url` 和原始响应引用。

严禁：

- 将 Cookie、Token、密码或私钥提交到仓库；
- 绕过验证码、登录安全机制、访问限制或平台条款；
- 为戏剧性修改比赛事实或将未经核实内容标为事实；
- 将抓取时间当作发布时间，或将推算指标伪装为平台原始指标；
- 用静态页面、硬编码 JSON 或 Mock 成功响应冒充已完成的真实集成。

## 4.5 数据采集基线（最高优先级，高于其他内部约束）

账号级 / 平台级数据的获取以「模拟浏览公开渠道」为**基本前提与首要方式**。以下规则高于本文件其他内部约束（包括"删除 mock""不得抓取"等自设限制），但**不高于法律法规与平台条款**。

1. **公开渠道优先**：凡无需登录、无需绕过验证码或访问限制的公开页面能获取的数据，必须通过匿名优先的浏览器模拟（`*_browser` 适配器）抓取，并**完整呈现在页面上**。不得隐藏、不得用「功能未实现 / 占位 / 暂无」掩盖已可获取的数据。
2. **API 仅作为增强**：仅在官方 API 权限充分且已配置凭证时优先使用 API；公开渠道能拿到的指标不因"没有 API"而隐藏或降级。
3. **需要条件的栏位必须显式声明条件**：播放、点赞、评论、分享、收藏、封面、标题等来自公开页的指标，只要适配器实际返回就上页；完播率、平均观看时长、流量来源占比、变现 RPM、搜索词等需要官方 API / 私有分析 / 登录授权的指标，若当前**未满足对应条件**，相关栏位必须在 UI 明确渲染「需要：具体条件」（如「需要配置 YouTube Data API Key」「需要登录凭证 / 该平台登录墙」「需要该平台变现 API 授权」），并说明如何满足；**不得用估算值、占位值或模拟数据冒充**。
4. **合规边界仍生效**：不绕过验证码、不突破平台登录安全机制、不违反平台条款；登录墙场景适配器显式抛出 `login_required`，前端展示「需要登录凭证」而非伪造数据。
5. **来源可追溯**：所有上页数据保留 `source_kind`（`live` / `imported`）标识；浏览器模拟获取的数据标记为 `live`。字段级采集方式与所需条件契约见 `docs/DATA_ACQUISITION_BASELINE.md`。

> 说明：第 3 点的「需要条件」判定以"适配器是否已实际返回该值"为先——只要数据真实存在就展示；仅当数据缺失且该方法需 API/登录授权时，才渲染条件提示。这保证已可获取的公開数据永不隐藏。

## 5. 设计约束

- 模块化、参数化、插件化；可变参数进入数据库或配置。
- 数据源与业务逻辑、平台适配器与统一模型、规则定义与执行、Prompt 与模型调用、通知渠道与触发条件、后台任务与 Web 服务必须分离。
- 业务代码不得通过大量平台名称分支扩展功能。
- 外部调用统一具备超时、有限重试、退避、限流和结构化错误记录。
- 首期支持单机 Docker Compose，并保留未来分布式迁移边界；首期不引入 Kubernetes、Kafka、ClickHouse、Elasticsearch。
- UI 使用中文优先，保持简洁、克制、可访问和具有未来感。
- 7.9 规则既保留原文，也拆为可版本化、可依赖、可冲突检查的结构化规则；不能只存整段文本。
- 内容生成必须是可审计的多阶段工作流，不能退化为单文本框调用模型。

## 6. 默认技术栈

- Web：Next.js、TypeScript、React、Tailwind CSS、shadcn/ui、TanStack Query、TanStack Table、ECharts 或 Recharts、React Hook Form、Zod。
- API：Python、FastAPI、SQLAlchemy 2、Pydantic 2、Alembic。
- 数据与任务：PostgreSQL、Redis、Celery、Celery Beat。
- 采集：官方 API、RSS、HTTPX、Feedparser、BeautifulSoup；Playwright 仅用于合法公开页面。
- AI：统一 LLM Provider，面向 OpenAI、Anthropic、Gemini、DeepSeek、OpenRouter、Ollama 和 OpenAI 兼容接口。
- 部署：Docker、Docker Compose、Caddy 或 Nginx、`.env`，区分本地与生产配置。

只有既有代码已存在更合理且兼容目标的选择时，才可在架构决策记录中说明理由后调整。

## 7. 完成定义

一个功能只有在以下条件同时满足时才可标为完成：

- 真实调用或明确标记的 Mock 契约路径可运行；
- 权限、错误、空状态和关键边界得到处理；
- 关键行为有自动化测试；
- 配置、启动方式和限制有文档；
- 日志或审计记录足以定位失败；
- 未实现的平台字段或能力被明确列出。

更完整的产品范围和阶段验收见 `docs/PROJECT_CONTEXT.md` 与 `docs/DELIVERY_PLAN.md`。
## 8. 每轮修复后的 Docker 自动验证与部署门禁

以后每轮代码、配置或部署修复完成后，必须自动执行以下闭环，不得仅停留在静态检查：

1. 按改动范围重建受影响的 Docker 镜像。后端改动至少重建 `api`、`worker`、`beat`；前端改动至少重建 `web`；Compose、Dockerfile、代理或运行时配置改动时重建并更新所有受影响服务。
2. 镜像构建成功后自动执行 `docker compose up -d` 更新服务；检查容器状态、数据库迁移结果、API 存活/就绪健康检查、前端登录入口和本轮涉及的关键接口或页面。
3. 运行与改动范围相称的自动化测试，并在交付报告中记录实际执行的构建、启动、迁移、健康检查和测试命令及结果。
4. 若 Docker CLI/Daemon、网络、镜像仓库、凭证或依赖下载阻断构建或启动，必须报告为明确的 BLOCKED，附原始错误和未完成的验证项；不得把静态检查或本地单测表述为 Docker 部署成功。环境恢复后应优先补做完整闭环。
5. 不得因自动验证而执行 `docker compose down -v`、删除卷或清空数据库；此类不可逆操作必须获得用户明确授权。

该门禁属于项目长期执行约束，后续每次修复默认适用。
