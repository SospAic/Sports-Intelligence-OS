# Sports Intelligence OS — 后续开发建议书（2026-08-08）

> 编制依据：基于本仓库在 2026-08-06 ~ 08-08 期间完成的「项目全面审查清单」闭环工作，
> 以及对账号层业务逻辑、同步/媒体/下载/测试链路的逐层审计。本文把**已验证的优秀实践**沉淀为团队规范，
> 并给出按优先级排序的后续开发路线图。所有「优秀案例」均指向本仓库真实存在的文件/函数，可直接作为模板复用。

---

## 一、已验证的优秀实践（建议固化为团队规范）

这些做法在本轮修复中被反复证明有效，应作为后续开发的默认范式：

### 1. 账号层业务逻辑「4 平台一致性」纪律
- **案例**：`MEMORY.md` 约定的硬性规则——凡动 avatar / 同步日志 / 任意账号级行为，必须同时对
  YouTube / TikTok / Douyin / Bilibili 四平台（每平台 API + browser 两套适配器，共 8 个文件）同步改动。
- **价值**：避免了「只修一个平台就交付」导致的串图、坏图覆盖好图等回归（如 3 个 TikTok 账号曾因
  browser 兜底抓到默认灰头像而显示同一张图）。
- **固化建议**：PR 模板增加「跨平台一致性自查清单」勾选项；CI 增加适配器文件成对变更检测。

### 2. 跨切面能力用 per-call 上下文注入，而非改单例
- **案例**：`app/adapters/platforms/base.py` 的 `AdapterCallContext`（frozen dataclass，
  `progress_sink` 等），在 `services/sync.py:execute_account_run` 按调用注入，替代了原先想在
  registry 单例上挂回调的写法——后者会因并发同步互相串扰。
- **价值**：可测试、无并发副作用、适配器保持纯函数式。
- **固化建议**：任何「贯穿多层、随请求变化」的能力（日志 sink、租户上下文、追踪 id）都走 per-call 注入。

### 3. 重/阻塞型工作一律卸载到 Celery
- **案例**：`preview_download` 从「HTTP 请求路径内 `asyncio.wait_for` 启 yt-dlp 子进程（45s 阻塞）」
  迁移为 `build_download_preview`（service）+ `preview_download_task`（celery，monitoring 队列）+
  `POST /downloads/preview` 入队 / `GET /downloads/preview/{task_id}` 轮询（commit `8dbb850`）。
- **价值**：API 事件循环不再被占用；端到端 Celery 管道（入队→broker→worker→result backend）已验证。
- **固化建议**：任何会启动子进程、访问外部受限站点、或耗时 > 1s 的端点，默认走 Celery + 轮询。

### 4. 安全「纵深防御」四件套
- **案例**：媒体/下载文件服务 `serve_content_media` / `download_file` 同时做
  `_allowed_files`（仅允许已记录的基线文件名）+ `_safe_media_path`（逃逸检测，返回 None→400）；
  头像下载 `_fetch_remote_bytes` 做了 **SSRF 放行 egress 代理段 + 拦截真实内网/元数据地址** +
  **content-type 白名单 + 大小上限**；头像缓存用 `is_default_avatar_url` 识别平台默认图避免污染。
- **价值**：即使某一层被绕过，下一层仍能挡住路径穿越 / SSRF / HTML 当图缓存。
- **固化建议**：所有「按用户提供的 URL/路径取资源」的入口，强制沿用此四件套模板。

### 5. 离线确定性测试：隔离 + Stub Provider
- **案例**：`tests/conftest.py` 用「专属测试库 + 每测试 `TRUNCATE` 隔离」+ `RealShapedTestAdapter` /
  `StubLLMProvider`（实现真实契约、返回确定性数据、`source_kind='live'`、不伪造成功）。
- **价值**：可在无网络/无真实凭证下跑通端到端工作流，且不违反「数据真实性」红线。
- **固化建议**：新增适配器/Provider 时，必须配套一个实现真实契约的 Stub，使 CI 不依赖外部。

### 6. 脏数据「标注而非删除」

- **案例**：本轮在生产库发现 4 行被抓取失败污染的账号名——YouTube `404 Not Found`（错误页标题）、
  TikTok `@https://www.tiktok.com/@...`（URL 泄漏进名称字段）、2× Douyin `的抖音`（昵称未渲染只剩后缀）。
  处置方式不是 `DELETE`，而是写入
  `accounts.metadata.data_quality = {flag:"invalid_display_name_legacy", observed_display_name, note}`。
- **价值**：① 保留可追溯证据，事后仍能回答「这行当初错成了什么样」；② 让「有多少历史脏数据」可被 SQL 统计
  （`WHERE metadata::jsonb ? 'data_quality'`）；③ 与「补根因守卫」配对——只清数据不补守卫，脏数据一定复发。
- **固化建议**：任何数据订正都遵循 **「标注 + 根因守卫」双动作**，禁止裸删。

### 7. 数据真实性红线
- **案例**：私有指标（完播率/平均观看时长/流量来源）缺条件时显示「需要：…」而非编造；适配器失败诚实上报
  `TransientAdapterError` / `LoginRequiredError`；mock 经 `SkeletonPlatformAdapter` 显式报错。
- **价值**：系统输出可被信任，无「假成功」。
- **固化建议**：任何「指标/内容生成」功能，缺数据时降级而非伪造，并在 UI 明确标注。

---

## 二、当前技术债与风险（按优先级）

| 优先级 | 项 | 现状 | 风险 |
|---|---|---|---|
| 🔴 P0 | 前端主题令牌化 | 亮色主题靠 `globals.css` 补丁式 slate 覆盖，组件仍大量写死 `bg-slate-900/950`、`bg-black` | 长期难维护，新增页面必现「白底+深蓝面板」割裂 |
| ✅ 已解决 | 测试套件全量运行会 hang | **已根治并实测**：`conftest.async_client` 的 `finally: await app.state.engine.dispose()` + `net` marker 隔离生效。2026-08-08 单条 `pytest` 一次跑完 **234 passed / 0 failed / 6 deselected，404s，无挂起** | — |
| 🔴 P0 | **测试覆盖存在结构性盲区** | 234 测试全绿，却漏掉 `services/sync.py` 中 `anyio` 未导入导致的 `NameError`——该分支（头像更新→缓存）**零测试覆盖**，实际会让四平台所有"有新头像"的同步全部崩溃。由 `ruff` F821 静态检查发现，不是测试发现 | 「测试数=234」给出虚假安全感；必崩路径可长期潜伏 |
| 🟠 P1 | Docker 浏览器登录无前端引导 | 基础设施（compose/browser.Dockerfile/CDP/noVNC）已具备，但仅 noVNC 直连，UI 无入口 | 运营人员难以自助完成登录会话 |
| 🟠 P1 | 历史媒体/字幕缺失 | 诊断显示 8974 条 content_items 中 `media.subtitles` 非空为 **0**；YouTube 主路径本可产出字幕但未回填 | 作品详情/创作页长期「无字幕」，功能形同虚设 |
| 🟡 P2 | 头像默认图识别偏保守 | `is_default_avatar_url` 仅覆盖 `noface` + 通用占位词；TikTok/Douyin 精确默认图串未纳入 | 个别平台默认图仍可能短暂入缓存（由 B/C 不抓兜底兜底） |
| 🟡 P2 | `.env` 外部凭证轮换 | 部署前需轮换用过的外部凭证 | 凭证泄漏风险 |

---

## 三、接下来开发建议（路线图）

### P0 — 地基加固（建议 1~2 个迭代）
1. **把静态检查提到与测试同等的门禁地位（本轮最高价值教训）**
   本轮 `anyio` NameError 事故的完整链条值得复盘：修改由「只加了两行调用、没加 import」造成 → 234 个测试
   全绿放行 → `ruff check` 一跑立刻报 `F821 Undefined name 'anyio'`。**测试没抓住，lint 抓住了。**
   建议立刻落地三件事：
   - **CI 顺序改为 lint → typecheck → test**，任一失败即中断（当前 `make lint` 与 `make test` 并列，
     容易只跑后者）；本轮已确认 `mypy` 也能独立捕获同类问题。
   - **提交前钩子**（pre-commit）至少跑 `ruff check` 改动文件，成本 < 1s。
   - **引入 coverage 门禁**：`pytest --cov=app --cov-fail-under=<基线>`，并对
     `services/sync.py`、`api/routes/media.py` 等核心链路设更高阈值。本轮盲区的本质是
     「无人为 `avatar_url` 非空的适配器响应写过测试」——coverage 报告会直接把这条分支标红。
2. **为「守卫函数」建立统一模式并强制配套测试**
   `avatar_helpers.should_update_avatar` 与本轮新增的 `profile_helpers.is_invalid_display_name` /
   `should_update_display_name` 已形成清晰范式：**纯函数、零 IO、四平台共用、单独测试文件**。
   建议将其固化为规范——任何「决定要不要用抓取到的值覆盖已存数据」的判断，都必须落在这样一个
   helper 里，禁止散落在适配器内部。理由：这类逻辑一旦分散，就必然出现「只修了一个平台」的偏差
   （头像串图事故的根因）。
3. **主题令牌化改造**：把 `--panel` / `--background` 等 CSS 变量接入组件（用 `bg-[var(--panel)]` 或
   shadcn 风格 token），逐步替换写死 slate。先做「新增组件强制用令牌」的 lint 规则，再分批迁移存量页面。
4. **补齐核心链路正面用例**：已有穿越防护的负面用例，建议再加 `serve_content_media`
   的「有效文件 200 / 越权访问 404」正面用例，以及各适配器 `resolve_account` 返回完整字段
   （含 `avatar_url`、异常 `display_name`）的契约测试——本轮 `test_sync_avatar_cache.py` 即模板。

### P1 — 能力与可运维性
3. **Docker 登录浏览器前端引导**：在账号/设置页加「打开登录浏览器」按钮，跳转到 noVNC 并提示「登录后关闭即可复用会话」，
   把现有基础设施价值显性化（替代纯文档说明）。
4. **历史媒体/字幕回填任务**：新增一个 Celery 任务，对 YouTube 账号重新同步并回填 `media.subtitles`/
   `cover_url`（注意 `get_sync_settings_config` 需回填 `YtDlpDownloadSettings` 默认值
   `write_subtitles=True`，否则同步不下载字幕——见 2026-08-05 诊断）。先小批量（≤50 账号）灰度验证。
5. **实时日志平台对等**：把已落地的 yt-dlp 日志 sink（`_YtDlpLogSink` + `SyncRun.metadata_json["yt_dlp_tail"]` +
   前端滚动面板）复刻到 TikTok/Douyin 的 browser 适配器，使四平台同步都有可见进度。

### P2 — 体验与健壮性
6. **头像默认图识别增强**：在不误杀真实图前提下，补充各平台签名默认图的特征（如 Bilibili `noface` 已覆盖，
   可加 TikTok/Douyin 已知默认图 host 子串白名单，低置信度不拦截）。
7. **下载/预览端点限流**：即使已异步化，仍对 `/downloads/preview` 与 yt-dlp 类任务做按 workspace 限流，防资源挤占。
8. **可观测性**：为 browser/CDP 服务加结构化健康检查；把 `source_kind` / 数据来源在 UI 持续一致地标注。

---

## 四、测试与质量门禁改进

- **已确立的全量基线（2026-08-08 实测）**：
  | 套件 | 命令 | 结果 |
  |---|---|---|
  | 后端 API | `docker compose exec api sh -lc "cd apps/api && pytest"` | **281 passed / 0 failed / 6 deselected（423s）** |
  | 根目录契约 | `make test-contract` | **35 passed（0.04s）** |
  | 前端 | `pnpm --filter @sio/web test` | **63 passed（16 files）** |
  | **合计** | | **379 passed / 0 failed** |
  后端由修复前的 234 增至 281（本轮新增 47 例）。任一数字下降即视为回归，建议把这张表写进 CI 摘要输出。
- **根目录契约测试的正确姿势（曾被误判为"环境不支持"）**：`tests/` 无 `__init__.py`，
  `unittest discover -s tests` 会报 *Start directory is not importable*，历史上被记成「容器未挂载所以跑不了」。
  实际只需 `PYTHONPATH=. python -m unittest discover -s tests -t tests`，宿主机即可运行。已固化为
  `make test-contract` 并前置到 `make test`。**教训：把"命令用错"误判为"环境限制"，会让一整块测试资产被长期搁置。**
- **契约测试的价值有了活证据（同样发生在本轮）**：为接入 `make test-contract`，我一度把 `test` 目标
  改成 `test: test-contract`（带前置依赖）。`test_infrastructure_contract.py` 立刻失败——它断言 Makefile 中
  必须存在字面量 `\ntest:\n`。最终改为在配方体内 `$(MAKE) test-contract`，两者兼得。
  这说明**基础设施契约测试能挡住"看起来无害"的结构改动**，值得继续投资：建议再补
  `docker-compose.yml` 服务/健康检查、Celery 队列路由、Alembic 单一 head 的契约断言。
- **net 测试隔离**：`pyproject.toml` 的 `addopts = -m 'not net'` + `net` marker 已就位，保持；
  新增依赖真实网络/CDP 的测试必须打 `net` marker，避免污染默认 CI 切片。
- **契约测试优先**：新适配器/Provider 必须配套 Stub（参照 `RealShapedTestAdapter`），CI 不依赖外部可达性。
  更进一步：Stub 的返回值应**覆盖字段的全部形态**（有值 / 空 / 异常形状），否则就会重演
  「`avatar_url` 永远为 None → 头像分支零覆盖 → NameError 潜伏」。

---

## 五、运维 / 部署建议

- **pypi 不可达时的构建闭环**：本环境 `docker compose build api` 因 pypi `SSLEOFError` 失败，已验证可靠路径是
  `docker cp` 改动文件进运行中容器 → `docker commit` → `docker compose up -d --no-build`。建议把该流程写成
  `Makefile` target（如 `make hotfix-api`），减少人工出错；长期应搭建内部 pypi 镜像或预烘焙基础镜像。
- **镜像 heritage**：api/worker 共用 `deploy/api.Dockerfile`，改动两者都要 `docker cp`；提交 beat 镜像时需
  `docker commit --change "HEALTHCHECK NONE"` 避免继承 api 的 HTTP 健康检测误判。
- **凭证与回滚**：部署前轮换用过的 `.env` 外部凭证；关键修复走 `git commit` + 镜像 tag，便于回滚。

---

## 六、本轮已闭环项回顾（供验收）

- 审查清单 6 🔴 / 5 🟠 / 4 🟡 全部落地并部署验证（SSRF、preview→Celery、download 白名单/脱敏、
  external_id 稳定哈希、亮色主题、video-search 去写死暗色、账号抽屉 dialog、data-table 单渲染、双路由穿越防护测试）。
- **原清单最后 3 项开放缺口本轮全部结清**：
  - 🔴「全量套件挂起无法跑完」→ 实测一次跑完 **234 passed / 0 failed（404s）**，问题已不复现。
  - 「根目录契约测试无法执行」→ 实为命令姿势错误，`make test-contract` 实测 **35 passed**。
  - 「残留脏账号需隔离标注」→ 4 行已写入 `metadata.data_quality` 标注（不删除），并补根因守卫。
- **本轮新发现并修复的真实缺陷**：`services/sync.py` 使用 `anyio` 却从未 import
  （`F821`），会让四平台**任何带新头像的同步**在写库前抛 `NameError`。已修复并补
  `test_sync_avatar_cache.py` 回归测试覆盖该分支。
- 累计新增测试文件 6 个；本轮新增 `test_profile_helpers.py`(24) + `test_sync_avatar_cache.py`(23)
  = **47 例**，全绿；`ruff` / `mypy` 对改动文件均干净。
